services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: bracelet
      POSTGRES_PASSWORD: bracelet
      POSTGRES_DB: bracelet
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
